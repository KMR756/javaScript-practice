"use strict"; // treat all JS code as newer version
let name = "Rahat"
let age = 20
let isLoogedIn = false









/**Note: dataType
 * code readability that should ne high\
 * **primitive dataType**
 * number => 2 to power 53
 * bigInt => for big number data
 * string => "for string" 
 * boolean => true/false
 * null => standalone value "represent a empty value" | typeof: object
 * undefined => value declered but not assign yet
 * symbol => finding uniqueness 
 * 
 * 
 * **non-premitive dataType**
 * Object => {key:"value"}
 * Array => [1,2,3,4] 
 * function => function user(name,age){}
 * Date => new Date()
 * RegExp => /abc/
 * map => new Map()
 * set => new Set()
 * weakMap => new WeakMap()
 * WeakSet => new WeakSet()
 * custom classes / instance => class Person {}
 */